# OpenAI API Configuration for Sora 2
# Copy this file to .env and fill in your API key

# Your OpenAI API Key
# Get it from: https://platform.openai.com/api-keys
# Note: The web GUI stores this in localStorage, not .env
OPENAI_API_KEY=sk-proj-your-api-key-here

# Optional: Default model preference
DEFAULT_MODEL=sora-2

# Optional: Default video duration (seconds)
DEFAULT_DURATION=10

# Optional: Default resolution
DEFAULT_RESOLUTION=1280x720

# Optional: Default quality
DEFAULT_QUALITY=standard

# Note: This .env file is for reference only.
# The web GUI doesn't read from .env files - it uses browser localStorage
# If you want to build a backend API, you can use these environment variables
